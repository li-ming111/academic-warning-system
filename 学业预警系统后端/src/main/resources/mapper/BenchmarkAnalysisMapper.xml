<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.academic.mapper.BenchmarkAnalysisMapper">

    <select id="getLatestAnalysis" parameterType="Long" resultType="com.academic.entity.BenchmarkAnalysis">
        SELECT * FROM benchmark_analysis 
        WHERE student_id = #{studentId}
        ORDER BY semester DESC 
        LIMIT 1
    </select>

    <select id="getAnalysisBySemester" resultType="com.academic.entity.BenchmarkAnalysis">
        SELECT * FROM benchmark_analysis 
        WHERE student_id = #{studentId} 
        AND semester = #{semester}
    </select>

    <select id="getClassAnalysis" resultType="com.academic.entity.BenchmarkAnalysis">
        SELECT * FROM benchmark_analysis 
        WHERE class_id = #{classId} 
        AND semester = #{semester}
        ORDER BY class_rank ASC
    </select>

    <select id="getMajorAnalysis" resultType="com.academic.entity.BenchmarkAnalysis">
        SELECT * FROM benchmark_analysis 
        WHERE major_id = #{majorId} 
        AND semester = #{semester}
        ORDER BY major_rank ASC
    </select>

    <select id="getHistoryAnalysis" resultType="com.academic.entity.BenchmarkAnalysis">
        SELECT * FROM benchmark_analysis 
        WHERE student_id = #{studentId}
        ORDER BY semester DESC
    </select>

    <select id="getClassAvgGpa" parameterType="map" resultType="Double">
        SELECT AVG(CAST(student_gpa AS DECIMAL(5,3))) 
        FROM benchmark_analysis 
        WHERE class_id = #{classId} 
        AND semester = #{semester}
    </select>

    <select id="getMajorAvgGpa" parameterType="map" resultType="Double">
        SELECT AVG(CAST(student_gpa AS DECIMAL(5,3))) 
        FROM benchmark_analysis 
        WHERE major_id = #{majorId} 
        AND semester = #{semester}
    </select>

</mapper>
